
<h1><%= @category.name %></h1>
 
<h3><%= pluralize(@category.cocktail_recipes.count, 'Cocktail Recipe') %></h3>
<ul>
  <% @category.cocktail_recipes.each do |c| %>
    <li><%= link_to c.title, cocktail_recipe_path(c) %></li>
  <% end %>
</ul>