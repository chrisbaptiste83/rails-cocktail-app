# Name of your application. Used to uniquely configure containers.
service: rails_cocktail_app

# Name of the container image.
image: chrisbaptiste83/rails_cocktail_app

# Deploy to these servers.
servers:
  web:
    - 172.236.243.75

# Enable SSL auto certification via Let's Encrypt and allow for multiple apps on a single web server.
proxy:
  ssl: true
  host: mikescantina.live
  healthcheck:
    interval: 3
    path: /up
    timeout: 120

# Credentials for your image host.
registry:
  username: chrisbaptiste83
  password:
    - KAMAL_REGISTRY_PASSWORD

# Inject ENV variables into containers (secrets come from .kamal/secrets).
env:
  secret:
    - RAILS_MASTER_KEY
  clear:
    RAILS_LOG_LEVEL: info

# Aliases are triggered with "bin/kamal <alias>".
aliases:
  console: app exec --interactive --reuse "bin/rails console"
  shell: app exec --interactive --reuse "bash"
  logs: app logs -f
  dbc: app exec --interactive --reuse "bin/rails dbconsole"

# Persist the SQLite database and Active Storage files across deployments.
volumes:
  - "cocktail_storage:/rails/storage"
  - "cocktail_db:/rails/db"

# Bridge fingerprinted assets between versions.
asset_path: /rails/public/assets

# Configure the image builder.
# Build remotely on the server to avoid local Docker buildx issues.
builder:
  remote: ssh://root@172.236.243.75
  arch: amd64
